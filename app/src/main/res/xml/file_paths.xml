<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Where AppUpdateInstaller writes the downloaded APK before handing it to the
         system installer. Must match ApkDownloader.updateFile()'s directory. -->
    <external-files-path name="updates" path="updates/" />
</paths>
